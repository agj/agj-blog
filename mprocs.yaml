procs:
  dev:
    shell: just qr && pnpm exec elm-pages dev --port 1234
    autostart: true
  review:
    shell: pnpm exec elm-review ./src ./app --compiler "./node_modules/.bin/lamdera" --watch --fix
    autostart: true
